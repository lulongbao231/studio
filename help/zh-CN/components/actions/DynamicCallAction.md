# DESCRIPTION

执行一个名称预先未知的用户动作，即其名称在 Flow 执行期间确定，例如其名称可以来自变量。这样的用户动作不得有输入和输出，只能有 _Start_ 和 _End_ Actions。

# PROPERTIES

## Action

要执行的用户动作的名称，通过在 Flow 执行期间对此表达式求值获得。

# INPUTS

## seqin

一个标准顺序输入。

# OUTPUTS

## seqout

一个标准顺序输出。

# EXAMPLES [EMPTY]
