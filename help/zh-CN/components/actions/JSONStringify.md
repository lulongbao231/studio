# DESCRIPTION

将 Flow `Value` 转换为 JSON 字符串，并发送到 `result` 输出。

# PROPERTIES

## Value

将转换为 JSON 字符串的 Flow 值。

## Indentation

生成的 JSON 字符串中使用的缩进。

# INPUTS

## seqin

一个标准顺序输入。

# OUTPUTS

## seqout

一个标准顺序输出。

## result

构造的 JSON 字符串通过此输出发送。

# EXAMPLES

- _JSON_
