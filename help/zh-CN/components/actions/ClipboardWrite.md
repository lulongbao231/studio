# DESCRIPTION

将通过 `data` 属性指定的数据写入剪贴板，该数据可以是文本或图像。

# PROPERTIES

## Data

写入剪贴板的数据。可以是 `string` 或 `blob` 类型。如果是 `blob` 类型，则假定它是图像。

# INPUTS

## seqin

一个标准顺序输入。

# OUTPUTS

## seqout

一个标准顺序输出。

# EXAMPLES [EMPTY]
