# DESCRIPTION

与串口建立连接。如果连接成功，Flow 执行通过 `seqout` 输出继续；如果发生错误，则可以捕获该错误（如果启用了 `Catch error`）。

# PROPERTIES

## Connection

用于串行通信的连接名称。

# INPUTS

## seqin

一个标准顺序输入。

# OUTPUTS

## seqout

一个标准顺序输出。

# EXAMPLES

-   _串口_
