# DESCRIPTION

将所设置的常量通过 `value` 数据输出传递。

# PROPERTIES

## Value

其结果被发送到 `value` 输出的表达式。此表达式不得使用变量。一些示例：

-   `"string"`
-   `42`
-   `3.14159265`
-   `true`
-   `Math.sin(0.5)`

# INPUTS

## seqin

一个标准顺序输入。

# OUTPUTS

## seqout

一个标准顺序输出。

## value

所设置的常量通过此数据输出传递。

# EXAMPLES [EMPTY]
