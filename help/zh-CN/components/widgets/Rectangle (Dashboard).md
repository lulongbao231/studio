# DESCRIPTION

此 Widget 使用 `Default` 样式中的背景色渲染一个实心矩形。它也可以用于渲染水平线（如果高度为 1 像素）和垂直线（如果宽度为 1 像素）。

# PROPERTIES

## Default style

渲染 Widget 背景时使用的样式。

# INPUTS [EMPTY]

# OUTPUTS [EMPTY]

# EXAMPLES

-   _仪表板部件演示_
